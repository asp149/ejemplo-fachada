import { Routes } from '@angular/router';
import { SceneComponent } from './features/scene/scene.component';

export const routes: Routes = [
    {
        path:"",
        component: SceneComponent,
        title: "3D Editor"
    }
];
